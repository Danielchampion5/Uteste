using UnityEngine;

public class exercicio1R : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int i = 5;
        while (i > 0)
        {
            print(i);

            i--;
        }

        print("Inicio da partida");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
